# patches/

One file for each change that belongs upstream rather than in this fork.

A file here is a report, not a patch to apply. It states the fault, how to
reproduce it, what it costs, and the smallest change that fixes it. The tree
itself stays unchanged, so that the upstream file keeps a zero diff until the
pull request lands.
